{
  "name": "seyva-stokvel",
  "private": true,
  "type": "module",
  "packageManager": "bun@1.3.13",
  "engines": {
    "node": ">=20"
  },
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "lint": "biome check --write .",
    "lint:ci": "biome ci .",
    "test": "turbo run test",
    "test:e2e": "playwright test",
    "test:e2e:ui": "playwright test --ui",
    "test:e2e:docker": "docker build -f Dockerfile.test -t seyva-test . && docker run --rm -v \"$PWD/tests\":/workspace/tests -v \"$PWD/test-results\":/workspace/test-results seyva-test",
    "test:e2e:update-snapshots": "docker build -f Dockerfile.test -t seyva-test . && docker run --rm -v \"$PWD/tests\":/workspace/tests -v \"$PWD/test-results\":/workspace/test-results seyva-test bun run test:e2e -- --update-snapshots",
    "typecheck": "turbo run typecheck",
    "format": "biome format --write .",
    "prepare": "husky"
  },
  "lint-staged": {
    "*.{ts,tsx,js,jsx,json,jsonc}": "biome check --write --no-errors-on-unmatched"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.4.14",
    "@playwright/test": "^1.59.1",
    "husky": "^9.1.7",
    "lint-staged": "^16.4.0",
    "turbo": "^2.9.9",
    "typescript": "^6.0.3"
  }
}
