# Local development — point at the docker-compose postgres service.
DATABASE_URL=postgres://seyva:seyva@localhost:5432/seyva

# Hono BFF listen port. Vite dev server proxies /api/* here.
PORT=3000

# NODE_ENV=development | production. Production strips the dev-only CORS allowance.
NODE_ENV=development
