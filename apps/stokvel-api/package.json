{
  "name": "@seyva/stokvel-api",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "bun --env-file=.env --watch src/index.ts",
    "build": "NODE_ENV=production bun build src/lambda.ts --outdir dist --target node --minify",
    "start": "bun dist/lambda.js",
    "typecheck": "tsc --noEmit",
    "test": "bun test"
  },
  "dependencies": {
    "@aws-sdk/client-ssm": "^3.700.0",
    "@seyva/db": "workspace:*",
    "@seyva/types": "workspace:*",
    "@seyva/utils": "workspace:*",
    "@seyva/validation": "workspace:*",
    "hono": "^4.12.12",
    "ua-parser-js": "^2.0.3"
  },
  "devDependencies": {
    "@types/ua-parser-js": "^0.7.39",
    "bun-types": "^1.2.0"
  }
}
