# Bun configuration. Defaults are fine for this monorepo.
# Workspace deps use the `workspace:*` protocol (declared per-package).

[install]
# Use semver ranges for external deps (default behaviour).
exact = false

[test]
# Define Vite-injected globals so app code under test doesn't blow up on them.
preload = ["./tests/preload.ts"]
