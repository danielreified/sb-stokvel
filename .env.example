# Root .env.example
#
# Most env vars are per-app. Copy each app's .env.example to .env when needed:
#   cp apps/stokvel-api/.env.example apps/stokvel-api/.env
#   cp apps/stokvel-app/.env.example apps/stokvel-app/.env
#
# Real .env files are gitignored.
